name: spindle_chat_example
description: Example app demonstrating the spindle_chat package.
version: 0.1.0
publish_to: none

environment:
  sdk: '>=3.7.0 <4.0.0'
  flutter: '>=3.29.0'

dependencies:
  flutter:
    sdk: flutter
  spindle_chat:
    path: ../
